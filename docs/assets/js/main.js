"use strict";const square=document.querySelectorAll(".js_square");let timeleft=document.querySelector("#timeLeft"),score=document.getElementById("score");const btn=document.querySelector(".js_btn"),reset=document.querySelector(".js_reset");let result=0,currentTime=timeleft.textContent,hitPosition=null,timerId=null,timeMole=null;function handleClick(e){e.preventDefault(),randomSquare(),time(),moveMole()}function randomSquare(){square.forEach(e=>{e.classList.remove("mole")});let e=square[Math.floor(9*Math.random())];e.classList.add("mole"),hitPosition=e.id}function moveMole(){timeMole=setInterval(randomSquare,700)}function countDown(){currentTime--,timeleft.textContent=currentTime,0===currentTime&&(clearInterval(timerId),clearInterval(timeMole),alert("Game Over! tu puntuación final es: "+result))}function time(){timerId=setInterval(countDown,700)}function handleReset(){timeleft.textContent=60,score.textContent=0,result=0,currentTime=60}btn.addEventListener("click",handleClick),square.forEach(e=>{e.addEventListener("click",()=>{e.id===hitPosition&&(result+=1,score.textContent=result)})}),reset.addEventListener("click",handleReset);